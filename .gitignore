# Credentials must never be committed. The ad-video skill reads keys from
# ~/.config/ad-video/env, outside the repository, but guard against stray copies.
.env
*.env
env
.claude/skills/*/scripts/*.local.sh

# Generated ad assets
out/
